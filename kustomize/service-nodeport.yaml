apiVersion: v1
kind: Service
metadata:
  name: deploymenttest
spec:
  type: NodePort
  selector:
    app: deploymenttest
  ports:
    - name: http
      port: 8091
      protocol: TCP
      targetPort: 80   
      nodePort: 31000
